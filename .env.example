# Postgres connection string from Neon (Dashboard → Connection string → include ?sslmode=require)
DATABASE_URL=postgresql://user:password@ep-xxxx.neon.tech/payza_console?sslmode=require

# Port Render will inject automatically. Only needed for local dev.
PORT=3000

# Optional: lock the console behind a shared password (leave blank to disable)
CONSOLE_PASSWORD=
